import React, { useEffect, useState } from "react";
import { ActivityIndicator, Alert, Pressable, ScrollView, StyleSheet, View } from "react-native";
import AppScreen from "../../../components/layout/AppScreen";
import AppText from "../../../components/common/AppText";
import SettingsPageHeader from "../../../components/admin/settings/SettingsPageHeader";
import { getStaffProfile } from "../../../services/api/authService";
export default function SecurityScreen({ navigation }) { const [profile, setProfile] = useState(null); const [loading, setLoading] = useState(true); useEffect(() => { getStaffProfile().then(setProfile).catch((error) => Alert.alert("Security unavailable", error.response?.data?.message || "Unable to load account information.")).finally(() => setLoading(false)); }, []); return <AppScreen><ScrollView><SettingsPageHeader title="Security"><View style={styles.card}>{loading ? <ActivityIndicator color="#102A43" /> : <><AppText size="lg" weight="bold">Account security</AppText><AppText color="textSecondary" style={styles.text}>{profile?.email || profile?.userName || "Authenticated account"}</AppText><AppText color="textSecondary" style={styles.text}>Sessions, devices, login history, and two-factor settings are not provided by the current backend.</AppText><Pressable onPress={() => navigation.navigate("AdminChangePassword")} style={styles.button}><AppText style={styles.white}>Change Password</AppText></Pressable></>}</View></SettingsPageHeader></ScrollView></AppScreen>; }
const styles = StyleSheet.create({ card: { backgroundColor: "#FFFDF8", borderWidth: 1, borderColor: "#DED9CE", borderRadius: 18, padding: 16 }, text: { marginTop: 10, lineHeight: 21 }, button: { height: 44, borderRadius: 11, backgroundColor: "#102A43", alignItems: "center", justifyContent: "center", marginTop: 16 }, white: { color: "#FFFDF8" } });
